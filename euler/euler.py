#Método de Euler

input(float())


g = lambda x: 
    
    
def euler_simple(x_0, y_0, step):
    while keep_iter == True:
        
        x_
        
        
        keep_iter = bool(input("Do you want to keep iterating?\n Type True to continue, type False to stop: "))
        